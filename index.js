"use strict";
import * as _support from './src/index';
import _samples from './samples';
export default ({
    ..._support,
    samples:_samples
});
export const support = _support;
export const samples = _samples;
